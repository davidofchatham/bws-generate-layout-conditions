# Roadmap — bws-generate-layout-conditions

In-flight build tasks and deferred work.

- **Permanent contract** (invariants, bug ledger, signal map) → `architecture.md`
- **Decisions + rationale** → `adr/`
- **Working build state** → this file

---

## In-flight build tasks

Active change set 2026-07-08 (see `SPEC.md`), extended 2026-07-21 with the test-surface work (T12–T14) enabled by the wp-litespeed env. Order = row order. Prior set (condition split + membership, 2026-06-03) landed and trimmed.

T11 is sequenced before T10 deliberately: it characterizes the *current* V24/V25 render surface, which is the baseline T10's suppression is judged against. Doing T10 first leaves no before-state to compare to.

T15 → T16 is likewise deliberate (added 2026-07-29): the content-title Meaning-A flip **waits** for the archive-title signal so both title rules are named and shipped as one coherent surface. See the semantics caveat in T15 — the two signals do **not** want the same meaning, which is exactly why they need naming together rather than separately.

| id | st | Task | Cites | Description |
|---|---|---|---|---|
| T7 | x | Signal registry | V5, V8, V9, V27 | Canonical signal table in Detector: key → detector method, rule slug, positive label, `gp-no-*` class. `Condition::evaluate()`/`get_rules()` + body-class `$map` become loops over it. Persisted slugs + class names byte-identical before/after. |
| T8 | x | Featured-image config-replay non-singular | V22 | Extend `layout_element_disables()` to `_generate_disable_featured_image`; OR into featured-image signal when `! is_singular()`. Closes V22 gap; update architecture.md signal map + V22 on land. |
| T9 | x | Environment seam + tests | V1, V2, V5, V6 | Internal seam under Detector (~6 reads), WP/GP adapter + in-memory fake. New `composer.json` + PHPUnit. Tests exercise `states()` only. V1/V2/V5/V6 become executable. |
| T12 | x | Integration test surface (fixtures + seam fidelity) | V4, V22, V23, V26, V28 | T9 covers the Detector against the **fake**, by design — so nothing verified the fake's own assumptions about GP/GB internals. Adds `tools/fixtures/layout-states/` (13 pages + 6 elements, composing on core-structures v4) and `seam-fidelity.php`, 28 assertions against `BWS_GP_WP_Environment` on real WP/GP: `meta_query` `!=` semantics incl. deleted-row behaviour, V4 three-arg replay proven discriminating both ways, V23's `''` fatal proven real *and* its remedy, the V26 sidebar enum, and non-singular request state (V22/T8). Landed 2026-07-21 (`1be570b`), verified on GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T13 | x | V2 poisoned-signal proof | V2, V20 | Adds `poisoned-signal.php`, 20 assertions making V2 executable rather than documented. Proves the Block Element's unconditional `remove_action` (`class-block.php:169-175`) really does strip `generate_construct_header`/`_footer` on `ls-page-poisoned`, that hook-state *would* therefore report a false "disabled", and that config-replay reports active anyway — with an `ls-page-layout-disabled` control proving config-replay still discriminates. Order is load-bearing (`remove_action` is process-global, one process per run): control arm first, re-checked immediately before poisoning so a reorder fails loudly. Mutation-checked — reverting `is_header_disabled()` to hook-state fails exactly one assertion by name. Landed 2026-07-21, verified on GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T14 | x | Version-drift canary | V27, V28 | Adds `tools/probes/upstream-surface.php`, 28 assertions turning the read-by-eye `registry-shape.php` report into a pass/fail guard. Pins what the plugin actually binds to: `Registry::register()` static + 3-arg + param order, the full 5-method `GenerateBlocks_Pro_Condition_Interface` (highest-value — `register()` rejects a non-conforming class by **returning false**, so an added interface method would drop both conditions from the UI with no diagnostic anywhere), the abstract's concrete `get_operators_for_rule`/`sanitize_value` the plugin inherits, the `generateblocks_register_conditions` hook name, both V27 slugs registering end-to-end with operators intact and instantiating, and `show_data()` static + ≥3-arg (V4). Read-only, no global mutation, so order-independent. Runs **first** in `seed-all.sh` (per-plugin, deduped): when upstream breaks, the blueprint suites fail too but with local-looking messages, so naming the real cause up front saves the detour. Mutation-checked — three simulated drifts each failed in their own section, naming their own cause. Landed 2026-07-21, recorded against GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T11 | x | Render-level test harness (HTTP) | V12, V24, V25 | Adds `render-surface.sh`, 18 assertions on real HTTP responses, plus blueprint v2 fixture repair. **Found and fixed B5**: the CSS-neutralize had never run on any request — GP Premium requires its Disable Elements module at file scope, so the plugin's `plugins_loaded:5` definition always lost the `function_exists` race. Invisible to CLI (GP's implementation also returns `''` with no `$post`), which is precisely why this harness had to exist. Fixture repair was a prerequisite: three of four surfaces were unobservable — no featured image, no nav menus, and `generate_menu_plus_settings` written as a `theme_mod` when GP reads it only via `get_option()`, so **V25 had never once been observed** and any assertion would have passed vacuously. V24/V25 now confirmed empirically. Harness recycles lsphp before asserting (`opcache.revalidate_freq=120` renders stale bytecode over HTTP while wp-cli reads fresh — a mutation test passed 18/18 against a demonstrably broken plugin before this was added). Mutation-checked: reverting the load order fails exactly the three neutralize assertions by name. Landed 2026-07-21. Original scope: assert on **rendered output**, unreachable from wp-cli at any query state. Build on the env's `bin/smoke.sh` `fetch` helper (curl through the container, `--resolve` to 127.0.0.1) against the seeded `ls-page-metabox-*` fixtures, which already carry the `_generate-disable-*` keys. Characterizes the current V24/V25 surface: Featured Image + Secondary Nav are CSS-only (full regression surface), Primary Nav partial via `#mobile-header`. Prerequisite for T10 — establishes the before-baseline T10's after-state is judged against. |
| T10 | x | Approach B: PHP suppression for CSS-only toggles | V14, V24, V25 | Ported into `class-disable-elements.php` (alongside the neutralize — the two are halves of one mechanism): `has_nav_menu` filter (secondary nav), `remove_action` ×2 (featured image), `remove_action` (`#mobile-header`), keyed on `_generate-disable-*`, `wp:60`. Prototype skipped as a deploy step — the harness makes the mu-plugin/View-Source loop redundant. Harness extended to 26 assertions: the three V24/V25 assertions **inverted** (they characterized the regression; they now prove it closed) plus a 6-assertion over-suppression section. Blueprint → v3: `ls-page-metabox-{nav,secondary-nav}` gain thumbnails, without which the over-suppression checks passed vacuously. Mutation-checked ×3 — hook disabled fails exactly the 3 T10 assertions; wrong meta key and wrong `remove_action` priority each fail exactly 1, by name. **Two findings.** (1) The featured-image assertion first ran red against working code: it grepped the attachment *filename*, which also appears in `og:image`/`twitter:image`/Yoast JSON-LD regardless of render. Now matches the `page-header-image` wrapper; all filename markers replaced suite-wide. (2) The `$location` guard on the `has_nav_menu` filter is **defensive, not load-bearing, and not falsifiable here** — a `$location`-blind filter still passes 26/26, because `has_nav_menu` is called for exactly one location in all of GP + GP Premium (`'secondary'`, 12 sites; zero for `'primary'`, which renders unconditionally with a page-list fallback). Kept + documented in-place rather than left as an over-claiming assertion. GB Pro composition check **mechanized, not deferred**: double `remove_action` verified a silent no-op on testbed (`true`/`false`/`false`, no notice) and paired `has_nav_menu` filters are idempotent, so composition is OR-semantics by construction and needs no fixture. Backprop V14; Approach A obsolete. **Visual confirmation done (David, 2026-07-21)** — all three toggle pages eyeballed against `ls-page-baseline`, mobile width included, which was the one surface the harness structurally cannot reach (curl has no viewport). No layout collateral: removing a wrapper reflows content up cleanly, no collapsed gaps or orphaned bands. On `ls-page-metabox-nav` the `#mobile-header` row is gone while the Menu Plus grey bar correctly SURVIVES — a separate element, and its disappearance would have been the over-suppression failure section 5 tests for. Residual (accepted): fixtures carry a 1px image and a single-item nav, so a spacing regression that only appears under real content would not show; structural breaks would have. Landed 2026-07-21. |
| T15 | · | Page-title signal: fix the archive branch | V6, V21, V30 | **Rescoped 2026-07-29 (David) — no new rule.** First framing added `archive_title_active` as a second rule; the correct axis is page-structure **role**, not hook. `generate_show_title` straddles roles — page title on singular (`content-single.php:36`), *item* titles on archives (`content.php:35`, one per loop card) — while the archive's page title is a different hook entirely (`generate_archive_title` → `<h1 class="page-title">`, `archive.php:34`). Per **V6** this plugin reports page-level state only, so `content_title_active` must mean *the page title* on both page types: its archive branch changes from reading loop cards to reading the heading. Item titles are out of remit (`evaluate()` discards `post_id`, so only a global answer is even possible — a separate rule if ever scoped). **Collapses the two-rule plan:** rule count stays 10, no `gp-no-archive-title` class, persisted slug `content_title_active` untouched (V27, no migration), label may want clarifying (free — labels are not persisted). **Behaviour change — CLEARED 2026-07-29 (David):** on an archive `gp-no-content-title` currently reflects loop-card suppression and will start reflecting the heading, but no CSS on either deployed site keys on that class, so nothing consumes the old meaning. The class ships to any future install, so the change still belongs in the changelog; it is just not a migration risk here. **Detection is cheap and hook-state-clean:** `generate_archive_title` is a named function on a same-named action, so `has_action('generate_archive_title','generate_archive_title')` reads it directly — no config-replay, none of V29's named-callback blindness. Must AND with GP's own `generate_has_default_loop()` resolver: a **Loop Template** Block Element sets `generate_has_default_loop => __return_false` (`class-block.php:182`), which removes the entire `archive.php` default block *including* the archive-title `do_action` — a fourth suppression path that no `remove_action` scan would find. Both deployed sites run 2 loop templates each, so this path is live, not theoretical. **The bug that motivates it:** rows 4/5 suppress content title behind an `is_singular()` guard but remove the archive title **unguarded** (`class-block.php:296-302`), so on an archive the Detector reports `content_title_active = true` while the heading is actually gone — the exact inverse of the singular false-"disabled" that opened V21. Same element, same checkbox, opposite errors by page type. **Semantics caveat — the reason T16 waits, and a caveat on "consistent semantics":** on archives there is **no genuine-disable writer at all** (writer 3 doesn't touch it; the post metabox is singular-only, so it cannot reach an archive). Every archive-title suppressor is a relocation (rows 4/5/6) or a structural loop replacement. So Meaning A ("renders anywhere") is near-**vacuous** here — it would be true in almost every case — while Meaning B ("native slot renders") is precise and detectable. Archive title therefore wants **Meaning B**, the opposite of content title's flip. The two rules cannot be made semantically parallel; what T15/T16 must ship together is *coherent naming that does not imply they are*. Decide before either lands. Also in scope: GB Pro registers a `{{archive_title}}` dynamic tag (`generateblocks-pro .../dynamic-tags/class-register.php:39`) resolving via `single_cat_title()`/`single_term_title()`/etc., **not** through `get_the_archive_title` — so GP's `remove_filter` never touches it. It is the archive-side twin of the DTE gap in FW-58: renders independently, suppresses nothing. **Pairs with DTE `{{title}}` for free:** DTE's planned sitewide `{{title}}` covers both roles so one Page Hero template serves the whole site. A page-title signal that dispatches by page type is exactly its condition-side counterpart — no third rule, no author-side OR-composition (which would have mixed item titles with the heading and answered wrong on archives). **Open sub-call:** on an archive a Layout Element `_generate_disable_content_title` kills item titles but leaves GP's heading intact — should the dispatching rule honour that as author intent (false) or report the heading as it renders (true)? First matches what they clicked, second what the page shows. **Backprop on land:** the signal map's prospective archive row folds into the content-title row; V11's rule count is **unchanged** (10) — confirm before editing. |
| T16 | · | Content title → Meaning A (relocation ≠ disable) | V21, V29 | **Gated on T15** by David's call 2026-07-29 — ships with the archive-title rule so both title surfaces are named coherently in one pass. Flip `content_title_active` from Meaning B ("renders in the native slot", v1's shipped meaning) to Meaning A ("renders anywhere"), so a Page Hero that *embeds* the title no longer reads as a disable and blocks inside the Hero can be conditioned on it. Deployment-cleared: portals carries only Page Hero Block relocations, hargrave only Layout Element disables, zero overlap (survey in `architecture.md`), so the flip fixes portals and leaves hargrave byte-identical. **Scope cut 2026-07-29 — this is config-replay, not relocation-detection.** Meaning A = `NOT( genuinely disabled )`, and there are exactly two genuine sources: the post metabox `_generate-disable-headline` (writers 1+2 collapse to one key) and the Layout Element `_generate_disable_content_title` (writer 3). Relocations are never read — you stop consulting the poisoned hook instead of compensating for it, which is **structurally the same fix as V2/ADR-0001** for header/footer. Body becomes `post_metabox_disables() || layout_element_disables()`, byte-identical in shape to `is_header_disabled()`, both helpers already present. **Drops from scope:** all rows 5/6 detection — the `strpos`-on-element-content machinery, the `generate_page_header_get_options()` call, and its T14 canary line. **Closes V29 as a side effect** (reads the metabox key directly, no longer depends on GP Premium's redundant filter). Residue accepted: a Hero that disables the title and renders none reports active. Still a trap to note: the two element types use *different* keys for this signal (Layout `_generate_disable_content_title` vs Block/Hero `_generate_disable_title`) — replay must use the Layout one, and reading the Block key here would be wrong, not merely incomplete. Open sub-decisions: `gp-no-content-title` inherits whichever meaning is chosen and has no room to split, so CSS keyed on it changes silently; Meaning B becomes unanswerable by any rule afterwards (accepted — no consumer, slug still free); no fixture covers rows 5/6. |

---

## Deferred work

Not built until a concrete need is scoped.

| Item | Invariant | Description |
|---|---|---|
| ~~Content title: relocation vs disable~~ | V21, V29 | **Promoted to T15 + T16** (in-flight, 2026-07-29): the archive-title signal T15 uncovered is a separate rule, and the flip waits for it. Original framing preserved below for provenance. **Reframed 2026-07-29** after a complete writer survey (GP 3.6.1 / Premium 2.5.6 — full table in `architecture.md`). Original framing ("admin toggle: hook-state vs config-replay for Page Hero toggles") is **rejected on two counts.** (1) Scope was wrong: `generate_show_title` has six upstream writers, and two of them — legacy Page Hero and the Premium **Page Header** module, which is not a Page Hero at all — infer relocation from a `{{post_title}}` template tag in element content, with no toggle to key on. Any meta-keyed fix misses both. (2) A global toggle cannot express the need: one page can carry a Layout Element disable (genuine) *and* a Hero relocation (not) at once, requiring both meanings on the same request. Real problem is that "Content Title Active" conflates **"renders anywhere"** (wanted by blocks *inside* a Hero) with **"renders in the native slot"** (wanted by blocks outside it, and v1's shipped meaning) — opposite answers on the same page, and B-only fails *silently*. Candidate shapes: two rules, or flip to A and document the break. **Unblocked 2026-07-29** — deployed on ~2 sites, both David's, one of which is the site that exposed the issue; no external consumer of Meaning B exists, which **selects the flip to Meaning A** (the two-rule shape existed only to protect unknown B consumers, and would have cost four rules across both signals plus two dropdown names needing docs to disambiguate). Featured image is **not** symmetric (no template-tag writer) and may not need the same split. **Detection cost resolved** (see `architecture.md` rows 5/6): row 5 is an added meta read on the element query the Detector already runs; row 6 reuses `generate_page_header_get_options()` — verified side-effect-free and safe to call, so it is *not* the ~80-line reimplementation first estimated. Both need content reads the V28 seam does not yet expose. **Gate (a) CLEARED 2026-07-29** by measuring both deployed clones (survey table in `architecture.md`): portals carries only Page Hero Block relocations (2 elements), hargrave only Layout Element disables (3) — zero overlap, so the flip changes portals in the intended direction and leaves hargrave identical. Same pass found rows 5/6 unexercised across the deployment (`generate_package_page_header` option absent on both; no element contains `{{post_title}}`), so live writers are 2/3/4 only. **Implementation trap:** the two element types use *different* meta keys for the same signal — Layout `_generate_disable_content_title`, Block/Hero `_generate_disable_title` — so a meta-keyed fix must read both or silently miss one type (featured image shares one key across both, and is not affected). Detector reads no title meta today, so shipped code is unaffected. Remaining open, in order: (b) `gp-no-content-title` body class inherits whichever meaning is chosen and has no room to split, so any CSS keyed on it changes silently; (c) Meaning B ("is the native slot empty?") becomes unanswerable by any rule after the flip — accepted now, no consumer, slug still free if it returns; (d) no fixture covers rows 5/6. |
| ~~Layout Element featured image on non-singular~~ | V22 | **Promoted to T8** (in-flight, 2026-07-08). |
| ~~CSS-neutralize ("the fix") disable toggle — **Approach A: re-emit CSS**~~ | V14, V24, V25 | **Obsolete 2026-07-21 — T10 (Approach B) landed and passed.** Approach A was the fallback for if PHP suppression failed staging; it did not. The V14 regression is dissolved rather than papered, so there is nothing left for a re-emit toggle to re-emit. Kept for provenance only. Original: Add setting or filter to disable the CSS-neutralize independently. Per V24/V25 the regression surface is Featured Image + Secondary Nav (full) and Primary Nav's `#mobile-header` wrapper (partial), so a blanket "fix OFF when GB Pro absent" default is coarser than needed — a finer option is to keep neutralize ON but re-emit only the load-bearing rules from the original `generate_disable_elements()` body: `.generate-page-header, .page-header-image, .page-header-image-single` (featured image), `#secondary-navigation` (secondary nav), and `#mobile-header` (only when `$disable_nav` and Menu Plus mobile header active), while nulling the redundant header/footer/site-navigation rules. Default: fix OFF when GB Pro absent (safe, simple); fix ON when GB Pro present. Fix-only mode becomes a user-acknowledged opt-in rather than a hard regression. V14 needs updating when implemented. **Fallback only — obsolete if T10 (Approach B) passes staging.** |
| ~~Convert CSS-only toggles to PHP suppression — **Approach B (preferred)**~~ | V14, V24, V25 | **Promoted to T10** (in-flight, 2026-07-08; gated on staging validation). Mechanism detail preserved in T10 + `docs/proto/approach-b-snippet.php`. Original analysis: PHP suppression keyed on `_generate-disable-*` meta dissolves the V14 regression rather than papering it — Secondary Nav via `has_nav_menu` filter (GP's own pattern, `class-layout.php:534`), Featured Image via `remove_action` (`featured-images.php:96,114`), `#mobile-header` via `remove_action` (`generate-menu-plus.php:1070`). Only the 3 CSS-only surfaces — don't double-suppress header/footer/top-bar. |
| Container type condition | V8 | `gp_theme_container` condition (reserved slug, V27) for full-width vs contained. GP emits native body classes already; plugin layer justified only if a GB Pro condition on container type, non-singular coverage, or Customizer-default detection is needed. None currently scoped. |
