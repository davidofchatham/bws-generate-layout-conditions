# Roadmap — bws-generate-layout-conditions

In-flight build tasks and deferred work.

- **Permanent contract** (invariants, bug ledger, signal map) → `architecture.md`
- **Decisions + rationale** → `adr/`
- **Working build state** → this file

---

## In-flight build tasks

Active change set 2026-07-08 (see `SPEC.md`), extended 2026-07-21 with the test-surface work (T12–T14) enabled by the wp-litespeed env. Order = row order. Prior set (condition split + membership, 2026-06-03) landed and trimmed.

T11 is sequenced before T10 deliberately: it characterizes the *current* V24/V25 render surface, which is the baseline T10's suppression is judged against. Doing T10 first leaves no before-state to compare to.

T15 → T16 was sequenced deliberately (added 2026-07-29), then **merged into one change and landed together 2026-08-04** (ADR-0005). The sequencing rationale — ship both title surfaces as one coherent naming pass — became a merge once the archive branch turned out to be a constant rather than a second signal: shipping the archive fix alone would have landed an interim release where the rule means one thing on singular and another on archives, a mirrored version of the inconsistency the change exists to remove. One commit, one changelog entry. The T15 "semantics caveat" (that the two branches want opposite meanings) is **superseded** — see ADR-0005.

| id | st | Task | Cites | Description |
|---|---|---|---|---|
| T7 | x | Signal registry | V5, V8, V9, V27 | Canonical signal table in Detector: key → detector method, rule slug, positive label, `gp-no-*` class. `Condition::evaluate()`/`get_rules()` + body-class `$map` become loops over it. Persisted slugs + class names byte-identical before/after. |
| T8 | x | Featured-image config-replay non-singular | V22 | Extend `layout_element_disables()` to `_generate_disable_featured_image`; OR into featured-image signal when `! is_singular()`. Closes V22 gap; update architecture.md signal map + V22 on land. |
| T9 | x | Environment seam + tests | V1, V2, V5, V6 | Internal seam under Detector (~6 reads), WP/GP adapter + in-memory fake. New `composer.json` + PHPUnit. Tests exercise `states()` only. V1/V2/V5/V6 become executable. |
| T12 | x | Integration test surface (fixtures + seam fidelity) | V4, V22, V23, V26, V28 | T9 covers the Detector against the **fake**, by design — so nothing verified the fake's own assumptions about GP/GB internals. Adds `tools/fixtures/layout-states/` (13 pages + 6 elements, composing on core-structures v4) and `seam-fidelity.php`, 28 assertions against `BWS_GP_WP_Environment` on real WP/GP: `meta_query` `!=` semantics incl. deleted-row behaviour, V4 three-arg replay proven discriminating both ways, V23's `''` fatal proven real *and* its remedy, the V26 sidebar enum, and non-singular request state (V22/T8). Landed 2026-07-21 (`1be570b`), verified on GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T13 | x | V2 poisoned-signal proof | V2, V20 | Adds `poisoned-signal.php`, 20 assertions making V2 executable rather than documented. Proves the Block Element's unconditional `remove_action` (`class-block.php:169-175`) really does strip `generate_construct_header`/`_footer` on `ls-page-poisoned`, that hook-state *would* therefore report a false "disabled", and that config-replay reports active anyway — with an `ls-page-layout-disabled` control proving config-replay still discriminates. Order is load-bearing (`remove_action` is process-global, one process per run): control arm first, re-checked immediately before poisoning so a reorder fails loudly. Mutation-checked — reverting `is_header_disabled()` to hook-state fails exactly one assertion by name. Landed 2026-07-21, verified on GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T14 | x | Version-drift canary | V27, V28 | Adds `tools/probes/upstream-surface.php`, 28 assertions turning the read-by-eye `registry-shape.php` report into a pass/fail guard. Pins what the plugin actually binds to: `Registry::register()` static + 3-arg + param order, the full 5-method `GenerateBlocks_Pro_Condition_Interface` (highest-value — `register()` rejects a non-conforming class by **returning false**, so an added interface method would drop both conditions from the UI with no diagnostic anywhere), the abstract's concrete `get_operators_for_rule`/`sanitize_value` the plugin inherits, the `generateblocks_register_conditions` hook name, both V27 slugs registering end-to-end with operators intact and instantiating, and `show_data()` static + ≥3-arg (V4). Read-only, no global mutation, so order-independent. Runs **first** in `seed-all.sh` (per-plugin, deduped): when upstream breaks, the blueprint suites fail too but with local-looking messages, so naming the real cause up front saves the detour. Mutation-checked — three simulated drifts each failed in their own section, naming their own cause. Landed 2026-07-21, recorded against GB Pro 2.7.0-beta.1 / GP Premium 2.5.6. |
| T11 | x | Render-level test harness (HTTP) | V12, V24, V25 | Adds `render-surface.sh`, 18 assertions on real HTTP responses, plus blueprint v2 fixture repair. **Found and fixed B5**: the CSS-neutralize had never run on any request — GP Premium requires its Disable Elements module at file scope, so the plugin's `plugins_loaded:5` definition always lost the `function_exists` race. Invisible to CLI (GP's implementation also returns `''` with no `$post`), which is precisely why this harness had to exist. Fixture repair was a prerequisite: three of four surfaces were unobservable — no featured image, no nav menus, and `generate_menu_plus_settings` written as a `theme_mod` when GP reads it only via `get_option()`, so **V25 had never once been observed** and any assertion would have passed vacuously. V24/V25 now confirmed empirically. Harness recycles lsphp before asserting (`opcache.revalidate_freq=120` renders stale bytecode over HTTP while wp-cli reads fresh — a mutation test passed 18/18 against a demonstrably broken plugin before this was added). Mutation-checked: reverting the load order fails exactly the three neutralize assertions by name. Landed 2026-07-21. Original scope: assert on **rendered output**, unreachable from wp-cli at any query state. Build on the env's `bin/smoke.sh` `fetch` helper (curl through the container, `--resolve` to 127.0.0.1) against the seeded `ls-page-metabox-*` fixtures, which already carry the `_generate-disable-*` keys. Characterizes the current V24/V25 surface: Featured Image + Secondary Nav are CSS-only (full regression surface), Primary Nav partial via `#mobile-header`. Prerequisite for T10 — establishes the before-baseline T10's after-state is judged against. |
| T10 | x | Approach B: PHP suppression for CSS-only toggles | V14, V24, V25 | Ported into `class-disable-elements.php` (alongside the neutralize — the two are halves of one mechanism): `has_nav_menu` filter (secondary nav), `remove_action` ×2 (featured image), `remove_action` (`#mobile-header`), keyed on `_generate-disable-*`, `wp:60`. Prototype skipped as a deploy step — the harness makes the mu-plugin/View-Source loop redundant. Harness extended to 26 assertions: the three V24/V25 assertions **inverted** (they characterized the regression; they now prove it closed) plus a 6-assertion over-suppression section. Blueprint → v3: `ls-page-metabox-{nav,secondary-nav}` gain thumbnails, without which the over-suppression checks passed vacuously. Mutation-checked ×3 — hook disabled fails exactly the 3 T10 assertions; wrong meta key and wrong `remove_action` priority each fail exactly 1, by name. **Two findings.** (1) The featured-image assertion first ran red against working code: it grepped the attachment *filename*, which also appears in `og:image`/`twitter:image`/Yoast JSON-LD regardless of render. Now matches the `page-header-image` wrapper; all filename markers replaced suite-wide. (2) The `$location` guard on the `has_nav_menu` filter is **defensive, not load-bearing, and not falsifiable here** — a `$location`-blind filter still passes 26/26, because `has_nav_menu` is called for exactly one location in all of GP + GP Premium (`'secondary'`, 12 sites; zero for `'primary'`, which renders unconditionally with a page-list fallback). Kept + documented in-place rather than left as an over-claiming assertion. GB Pro composition check **mechanized, not deferred**: double `remove_action` verified a silent no-op on testbed (`true`/`false`/`false`, no notice) and paired `has_nav_menu` filters are idempotent, so composition is OR-semantics by construction and needs no fixture. Backprop V14; Approach A obsolete. **Visual confirmation done (David, 2026-07-21)** — all three toggle pages eyeballed against `ls-page-baseline`, mobile width included, which was the one surface the harness structurally cannot reach (curl has no viewport). No layout collateral: removing a wrapper reflows content up cleanly, no collapsed gaps or orphaned bands. On `ls-page-metabox-nav` the `#mobile-header` row is gone while the Menu Plus grey bar correctly SURVIVES — a separate element, and its disappearance would have been the over-suppression failure section 5 tests for. Residual (accepted): fixtures carry a 1px image and a single-item nav, so a spacing regression that only appears under real content would not show; structural breaks would have. Landed 2026-07-21. |
| T15 | x | Page-title signal: fix the archive branch | V6, V21, V30, V31 | **Landed 2026-08-04, merged with T16 into one change (ADR-0005).** The archive branch does **not** read `generate_archive_title` as planned below: no configuration source reaches the page-title role off singular (V31), so the signal short-circuits to not-disabled there. That drops the `generate_has_default_loop()` conjunct, the Loop Template path, and the whole archive-detection surface from scope — reading them would have re-opened the silent-failure direction Meaning A exists to close. The **role** analysis below is what survives and is now pinned as V31; the detection plan is superseded. Rule count unchanged at 10, slug and body class untouched, no migration — all confirmed on land. Backprop done: V21 closed for content title, V29 closed, V30 amended, V31 added, signal map + element toggle map rows rewritten, `render-surface.sh` §6 added (blueprint v4). Prior scoping below, retained for provenance. **Rescoped 2026-07-29 (David) — no new rule.** First framing added `archive_title_active` as a second rule; the correct axis is page-structure **role**, not hook. `generate_show_title` straddles roles — page title on singular (`content-single.php:36`), *item* titles on archives (`content.php:35`, one per loop card) — while the archive's page title is a different hook entirely (`generate_archive_title` → `<h1 class="page-title">`, `archive.php:34`). Per **V6** this plugin reports page-level state only, so `content_title_active` must mean *the page title* on both page types: its archive branch changes from reading loop cards to reading the heading. Item titles are out of remit (`evaluate()` discards `post_id`, so only a global answer is even possible — a separate rule if ever scoped). **Collapses the two-rule plan:** rule count stays 10, no `gp-no-archive-title` class, persisted slug `content_title_active` untouched (V27, no migration), label may want clarifying (free — labels are not persisted). **Behaviour change — CLEARED 2026-07-29 (David):** on an archive `gp-no-content-title` currently reflects loop-card suppression and will start reflecting the heading, but no CSS on either deployed site keys on that class, so nothing consumes the old meaning. The class ships to any future install, so the change still belongs in the changelog; it is just not a migration risk here. **Detection is cheap and hook-state-clean:** `generate_archive_title` is a named function on a same-named action, so `has_action('generate_archive_title','generate_archive_title')` reads it directly — no config-replay, none of V29's named-callback blindness. Must AND with GP's own `generate_has_default_loop()` resolver: a **Loop Template** Block Element sets `generate_has_default_loop => __return_false` (`class-block.php:182`), which removes the entire `archive.php` default block *including* the archive-title `do_action` — a fourth suppression path that no `remove_action` scan would find. Both deployed sites run 2 loop templates each, so this path is live, not theoretical. **The bug that motivates it:** rows 4/5 suppress content title behind an `is_singular()` guard but remove the archive title **unguarded** (`class-block.php:296-302`), so on an archive the Detector reports `content_title_active = true` while the heading is actually gone — the exact inverse of the singular false-"disabled" that opened V21. Same element, same checkbox, opposite errors by page type. **Semantics caveat — the reason T16 waits, and a caveat on "consistent semantics":** on archives there is **no genuine-disable writer at all** (writer 3 doesn't touch it; the post metabox is singular-only, so it cannot reach an archive). Every archive-title suppressor is a relocation (rows 4/5/6) or a structural loop replacement. So Meaning A ("renders anywhere") is near-**vacuous** here — it would be true in almost every case — while Meaning B ("native slot renders") is precise and detectable. Archive title therefore wants **Meaning B**, the opposite of content title's flip. The two rules cannot be made semantically parallel; what T15/T16 must ship together is *coherent naming that does not imply they are*. Decide before either lands. Also in scope: GB Pro registers a `{{archive_title}}` dynamic tag (`generateblocks-pro .../dynamic-tags/class-register.php:39`) resolving via `single_cat_title()`/`single_term_title()`/etc., **not** through `get_the_archive_title` — so GP's `remove_filter` never touches it. It is the archive-side twin of the DTE gap in FW-58: renders independently, suppresses nothing. **Pairs with DTE `{{title}}` for free:** DTE's planned sitewide `{{title}}` covers both roles so one Page Hero template serves the whole site. A page-title signal that dispatches by page type is exactly its condition-side counterpart — no third rule, no author-side OR-composition (which would have mixed item titles with the heading and answered wrong on archives). **Open sub-call:** on an archive a Layout Element `_generate_disable_content_title` kills item titles but leaves GP's heading intact — should the dispatching rule honour that as author intent (false) or report the heading as it renders (true)? First matches what they clicked, second what the page shows. **Backprop on land:** the signal map's prospective archive row folds into the content-title row; V11's rule count is **unchanged** (10) — confirm before editing. |
| T16 | x | Content title → Meaning A (relocation ≠ disable) | V21, V29, V31 | **Landed 2026-08-04 with T15 as one change (ADR-0005).** Body is `is_singular()` gate → `post_metabox_disables('_generate-disable-headline')` → `layout_element_disables('_generate_disable_content_title')`. Five new PHPUnit cases at the Detector's public state map (the highest available seam; the whole change lives beneath it), mutation-checked: the three that assert new behaviour were seen red by name before the fix, and removing the V31 gate fails exactly the off-singular case by name. The hook-state assertion in `test_hook_state_signals` was **inverted** into its own test rather than deleted, so the meaning flip guards itself. Rendered-response coverage added for the one claim the fake cannot validate — that GP leaves the archive heading standing under a Layout Element content-title disable (`render-surface.sh` §6, new `ls-el-layout-title-archive` fixture reusing the existing `department:sales` archive). **Run on testbed 2026-08-04: 33/33**, and the premise is now empirical rather than documented — on a live archive the `<h1 class="page-title">` heading is present while the loop-card `entry-title`s are gone. Mutation-checked: reverting the Detector to hook-state fails exactly the one `gp-no-content-title` assertion, by name, with the other 32 green. **That run also found B6** — both archive fixtures had been inert since v1 (term slug where GP compares term ID), so the V22 archive fixture had never applied to a request either; fixed at blueprint v4 with a `verify.php` §6 guard. Open sub-decisions below all resolved: the body class inherits Meaning A (disclosed in the changelog, cleared for both deployed sites), Meaning B is recorded as an accepted gap with a deferred entry, and rows 5/6 stay unfixtured **by design** — they are deliberately unread, not uncovered. Prior scoping below, retained for provenance. **Gated on T15** by David's call 2026-07-29 — ships with the archive-title rule so both title surfaces are named coherently in one pass. Flip `content_title_active` from Meaning B ("renders in the native slot", v1's shipped meaning) to Meaning A ("renders anywhere"), so a Page Hero that *embeds* the title no longer reads as a disable and blocks inside the Hero can be conditioned on it. Deployment-cleared: portals carries only Page Hero Block relocations, hargrave only Layout Element disables, zero overlap (survey in `architecture.md`), so the flip fixes portals and leaves hargrave byte-identical. **Scope cut 2026-07-29 — this is config-replay, not relocation-detection.** Meaning A = `NOT( genuinely disabled )`, and there are exactly two genuine sources: the post metabox `_generate-disable-headline` (writers 1+2 collapse to one key) and the Layout Element `_generate_disable_content_title` (writer 3). Relocations are never read — you stop consulting the poisoned hook instead of compensating for it, which is **structurally the same fix as V2/ADR-0001** for header/footer. Body becomes `post_metabox_disables() || layout_element_disables()`, byte-identical in shape to `is_header_disabled()`, both helpers already present. **Drops from scope:** all rows 5/6 detection — the `strpos`-on-element-content machinery, the `generate_page_header_get_options()` call, and its T14 canary line. **Closes V29 as a side effect** (reads the metabox key directly, no longer depends on GP Premium's redundant filter). Residue accepted: a Hero that disables the title and renders none reports active. Still a trap to note: the two element types use *different* keys for this signal (Layout `_generate_disable_content_title` vs Block/Hero `_generate_disable_title`) — replay must use the Layout one, and reading the Block key here would be wrong, not merely incomplete. Open sub-decisions: `gp-no-content-title` inherits whichever meaning is chosen and has no room to split, so CSS keyed on it changes silently; Meaning B becomes unanswerable by any rule afterwards (accepted — no consumer, slug still free); no fixture covers rows 5/6. |
| T17 | x | Secondary nav: config-replay the Layout Element layer | V32 | **Landed 2026-08-04.** Closes the last "not detected" gap that was purely a mechanism choice. Body becomes `post_metabox_disables('_generate-disable-secondary-nav') \|\| layout_element_disables('_generate_disable_secondary_navigation')` — same engine as header/footer, no new seam method, no new rule, slug and body class untouched. **The old comment was wrong about the difficulty, not just the priority:** it recorded "no clean hook signal … not detectable cleanly", which is true of hook-state and irrelevant to replay — the signal was reachable the whole time by the mechanism already in the file. Worth noting as a pattern: an accepted gap phrased in terms of one mechanism outlived the arrival of the other. Three PHPUnit cases (singular, off-singular, wrong-key control), mutation-checked ×2 — stubbing the replay branch fails exactly the two positive cases by name; swapping in the metabox key fails all three. **Deliberately ungated on `is_singular()`**: GP adds its `has_nav_menu` filter without a guard (`class-layout.php:311`), so the element disables on archives too — featured image's shape (V22), not content title's (V31). Not yet covered on rendered output: `render-surface.sh` has no secondary-nav Layout Element fixture, and the two-key trap is exactly the kind of thing the unit suite cannot falsify against a fake (B6's lesson). Add one when the blueprint is next touched — see the deferred entry below. |
| T19 | x | Featured image reports post-level intent | V7, V20, V21, V22, V33, B9 | **Landed 2026-08-06 (ADR-0006, GitHub issue #3).** `is_featured_image_disabled()` becomes one line — `post_metabox_disables('_generate-disable-post-image')` — so the rule reads no hook on any page type and the 0.2.0 non-singular replay is **reversed**. Label gains "(post setting)"; slug and body class untouched. The decision that needs carrying is the one that looks wrong at a glance: the Layout Element key is **not** replayed, unlike the content title's, because on real sites it is how a relocation is performed. Do not restore the symmetry. Six PHPUnit cases at the state map, two of them inversions of shipped assertions; `render-surface.sh` §8 (6 assertions, 40 → 46) inverting three more on rendered output. Mutation-checked ×2 at both levels: reintroducing the element read fails the archive assertion alone; reverting to hook-state fails the hero page and — the sharper one — the **baseline**, which is V33's "wrong out of the box" reproduced on testbed rather than argued from source. **Also found B9**: the Page Hero fixture had been inert since v1 (no `_generate_hook`, so GP never loaded the element), which nothing noticed because no assertion had ever read that page. Blueprint v7 — **note for the next blueprint touch:** issue #5 was written expecting to claim v7 itself; this change had to take it, so #5 lands at v8. Full run on testbed: 46 unit + 39 verify + 28 seam + 20 poisoned + 34 upstream + 46 render = **213 green**. One deviation from the ticket's acceptance wording, recorded rather than papered: it asks that reintroducing the element read fail "exactly the archive **and hero** assertions". It cannot — the hero page carries no Layout Element, so that mutation reaches the archive alone, and the hero assertion is guarded by the *other* mutation (reverting to hook-state), which also fails the baseline. Both mutations were run at both levels; the coverage is a superset of what was asked for, not a subset. **Substantially retired at blueprint v8 (T20), re-run 2026-08-06:** the same mutation now fails **4** assertions, two of them on a **singular** page, because `ls-page-featured-kill` is the Layout Element featured-image disable on singular that did not exist when this landed. What stays true is the structural half — the hero page's element is a *Block* Element, which `layout_element_ids()` filters out by type, so no mutation of this rule can ever reach it. Read the original wording as imprecise about which fixture, not as coverage still owed. |
| T18 | x | Render coverage for T17 + blueprint v5 | V32, V33, B7 | **Landed 2026-08-05.** Closes the "covered by the fake only" residue T17 shipped with. New `ls-el-layout-secondary-nav` element carrying **both** display conditions (a dedicated page + the `department:sales` archive), so one unguarded element proves both halves of V32 — GP applying the key it really writes, and the ungated reach onto an archive. `render-surface.sh` §7 (6 assertions, 33 → 39), `verify.php` §6 extended to three archive elements (35). Asymmetric by necessity: on singular both markup-absence and body class are asserted because the baseline is a control for the marker rendering at all; on the archive only the body class is, since the element *is* what disables it and no nav-intact archive exists here. **Mutation-checked ×2, and the second one found B7.** M1 (stub the replay branch) failed exactly the two Detector assertions by name with the GP-side one still green — correct, since GP's own behaviour is untouched. M2 (point the fixture at the metabox key) passed **39/39** — a false green: `seed.php` wrote meta additively, so the correct key from the previous seed was still on the element and GP kept reading it. Fixed by making the write authoritative (owned-prefix sweep); re-running M2 with no hand-deletion now fails 3 by name, the first naming the key. Full run on testbed: 41 unit + 35 verify + 28 seam + 20 poisoned + 39 render = **163 green**. **Also confirmed V33 live rather than from source** — on the `hargrave` clone the featured-image hook sits on `generate_after_header`, the probed hook is empty, a real thumbnail renders, and `states()['featured_image']` is `true`. The rule is wrong on every singular page of that site and has been since v1; `testbed`/`portals` store the keys unset, so GP's defaults produce the same misread there. |
| T20 | x | Kill-switch fixture + conditioned-block render coverage | V34, B6, B7, B8, B9 | **Landed 2026-08-06 (GitHub issue #5, under #2). Blueprint v8.** Two gaps close. (1) The case `featured_image_slot_active` exists for had no fixture: a Layout Element switching the featured image off on a **singular** page with **nothing drawing one in its place**. Every real instance pairs the removal with a hero or Content Template, so V34 parts (2) and (3) were argued from GP's source. `ls-el-layout-featured-kill` + `ls-page-featured-kill` are that case, and the page carries a thumbnail so the absent `page-header-image` wrapper is an observation rather than the default state of an image-less page. (2) **The authoring workflow itself.** Every render assertion before this one read markup the plugin or GP emits; none read the chain the plugin exists for — tick a toggle, and a block conditioned on the rule stops rendering. New `gblocks_condition` fixtures (`ls-cond-image-active`, `ls-cond-slot-active`) and three marker blocks carried verbatim by every row of the combination table: baseline (both active), the per-post toggle (both off), the Hero relocation, the kill switch, and the archive. On the archive they arrive via `ls-el-block-archive-markers`, a hook Block Element, since an archive has no `post_content`. The **unreachable** combination — post setting disabled, slot active — is asserted rather than omitted, on the only page where it could appear. `render-surface.sh` §9 (21 assertions, 46 → 67) and `verify.php` +28 (39 → 67), most of them in a new §8 but seven in the existing §1/§2/§6 loops, since the new fixtures belong to categories those already cover. Every row's liveness is a **hard-abort** on an unconditioned control marker, which is B9's lesson applied before the fact rather than after. `verify.php` §8 also **evaluates** both conditions rather than only checking their shape — on `ls-page-featured-kill` the two rules disagree, so one bootstrapped request yields both verdicts and no ordering hazard is introduced. **Mutation-checked ×6, one per fixture:** dropping the condition from a seeded block fails 4 render assertions and 5 verify ones by name; pointing the kill switch at the metabox key fails 2 and 1; removing the Page Hero's image toggle fails the hero row's two; removing the per-post toggle fails 4; stripping the marker element's `_generate_hook`, and dropping the baseline's marker blocks, each hard-abort. The `_generate_hook` one is the instructive one — `verify.php` §6 still reports that element "applies", because `show_data()` knows nothing about whether GP registered it. Exactly the B9 shape, and the reason the render-side control is not redundant with §6. Full run on testbed: 58 unit + 67 verify + 28 seam + 20 poisoned + 59 upstream + 67 render = **299 green**. **Residue, recorded:** V34 part (5)'s two false-active directions are still fake-only, because the harness pins the Blog module ON (B8) and they occur only with it off. Closing them needs a second harness run with the module deactivated, not another fixture. |
| T21 | x | Clone dry-run on real content, then release | V33, V34, ADR-0006 | **Ran 2026-08-07 (GitHub issue #6, under #2).** The fixtures prove the rules answer correctly against markup this project wrote; this is the first time the two featured-image rules were read against markup an author wrote, on the site that motivated the change. Target: the `hargrave` clone, plugin swapped to the dev tree with `bin/dev-plugin.sh --dev` (the clone runs an installed 0.2.1 copy, not the bind mount — the first `wire.php` run failed on `featured_image_slot_active is not registered`, which is that swap being missing, not a bug). **What was wired:** the real Content Template #47485 "Singular -- posts (news)" (`post:post`, draws title *and* image, pairs with Layout Element #78217 "Layout: Posts" carrying `_generate_disable_featured_image` — the relocation shape ADR-0006 is about). Its `generateblocks/media` block, `src="{{featured_image key:url}}"`, took a real `gblocks_condition` on `featured_image_active`; a second block took one on `featured_image_slot_active`; an unconditioned control marker rode along, B9's lesson. A temporary hook Block Element carried the same three markers onto the Athletics category archive, since a `post:post` content template cannot render on one and the archive row would otherwise have been argued from the wp-cli verdict alone. **Read via wp-cli, one process per page** (the Detector memoizes per request and the slot rule reads hook state, which a Layout Element mutates process-globally — two pages in one process report the first page's hooks for both): state map, both condition verdicts as GB Pro evaluates them, and the emitted body classes. **Five rows, 14 rendered assertions, all green.** (1) Post #52589, per-post toggle set: `featured_image` disabled, both conditions false, `gp-no-featured-image` present, the template's media block **absent** from the response and GP's own image absent too. (2) Post #78063, no toggle: post-setting rule active, the media block **renders** — the case that was broken before this work, on real content. (3) Page #2679, covered by Page Hero #71489: the hero's own image block renders (drawn through `{{image as:url|fallback:1485|use:featured}}` — the fallback-tag pattern V7 names, live), post-setting rule **active**, no body class. (4) The Athletics archive: the block conditioned on the post-setting rule **renders**, the slot marker does not, no body class — the removed 0.2.0 behaviour confirmed gone where it used to bite. (5) Page #74082, which nothing relocates: the slot rule answers **true** and GP's `page-header-image` really is in the response. Row 5 is the one the fixtures could not supply — every fixture and every other page here is a relocation, so the slot rule's positive direction had never been observed on real content. **Mutation-checked:** deleting `_generate-disable-post-image` from #52589 fails exactly 2 assertions by name (the hidden media block and the body class) and nothing else; restoring it returns 14/14. **Two marker traps hit, both the T10 finding-1 shape:** `gb-media-70d87efd` matches the block's own `css` attribute in the emitted stylesheet whether or not the block renders, so the assertion must match `class="gb-media-…` on the tag; and the Page Hero draws through a GB media block, not GP's `page-header-image` wrapper, so the wrapper marker was answering about the wrong mechanism. Both would have been false greens in the safe-looking direction. **One correction to a documented measurement:** the clone now stores `generate_blog_settings.single_post_image = true`, not `false` as the struck deferred entry recorded — the Customizer direction that entry weighed is not live on this clone today. The Blog module is active, so the rule reads the module path, as the harness pins. Clone left wired for the visual pass (David); revert is `bin/dev-plugin.sh --site hargrave bws-generate-layout-conditions --live`, which restores the pre-swap DB snapshot and takes the wiring with it. |

---

## Deferred work

Not built until a concrete need is scoped.

| Item | Invariant | Description |
|---|---|---|
| ~~Content title: relocation vs disable~~ | V21, V29 | **Promoted to T15 + T16** (in-flight, 2026-07-29): the archive-title signal T15 uncovered is a separate rule, and the flip waits for it. Original framing preserved below for provenance. **Reframed 2026-07-29** after a complete writer survey (GP 3.6.1 / Premium 2.5.6 — full table in `architecture.md`). Original framing ("admin toggle: hook-state vs config-replay for Page Hero toggles") is **rejected on two counts.** (1) Scope was wrong: `generate_show_title` has six upstream writers, and two of them — legacy Page Hero and the Premium **Page Header** module, which is not a Page Hero at all — infer relocation from a `{{post_title}}` template tag in element content, with no toggle to key on. Any meta-keyed fix misses both. (2) A global toggle cannot express the need: one page can carry a Layout Element disable (genuine) *and* a Hero relocation (not) at once, requiring both meanings on the same request. Real problem is that "Content Title Active" conflates **"renders anywhere"** (wanted by blocks *inside* a Hero) with **"renders in the native slot"** (wanted by blocks outside it, and v1's shipped meaning) — opposite answers on the same page, and B-only fails *silently*. Candidate shapes: two rules, or flip to A and document the break. **Unblocked 2026-07-29** — deployed on ~2 sites, both David's, one of which is the site that exposed the issue; no external consumer of Meaning B exists, which **selects the flip to Meaning A** (the two-rule shape existed only to protect unknown B consumers, and would have cost four rules across both signals plus two dropdown names needing docs to disambiguate). Featured image is **not** symmetric (no template-tag writer) and may not need the same split. **Detection cost resolved** (see `architecture.md` rows 5/6): row 5 is an added meta read on the element query the Detector already runs; row 6 reuses `generate_page_header_get_options()` — verified side-effect-free and safe to call, so it is *not* the ~80-line reimplementation first estimated. Both need content reads the V28 seam does not yet expose. **Gate (a) CLEARED 2026-07-29** by measuring both deployed clones (survey table in `architecture.md`): portals carries only Page Hero Block relocations (2 elements), hargrave only Layout Element disables (3) — zero overlap, so the flip changes portals in the intended direction and leaves hargrave identical. Same pass found rows 5/6 unexercised across the deployment (`generate_package_page_header` option absent on both; no element contains `{{post_title}}`), so live writers are 2/3/4 only. **Implementation trap:** the two element types use *different* meta keys for the same signal — Layout `_generate_disable_content_title`, Block/Hero `_generate_disable_title` — so a meta-keyed fix must read both or silently miss one type (featured image shares one key across both, and is not affected). Detector reads no title meta today, so shipped code is unaffected. Remaining open, in order: (b) `gp-no-content-title` body class inherits whichever meaning is chosen and has no room to split, so any CSS keyed on it changes silently; (c) Meaning B ("is the native slot empty?") becomes unanswerable by any rule after the flip — accepted now, no consumer, slug still free if it returns; (d) no fixture covers rows 5/6. |
| ~~Layout Element featured image on non-singular~~ | V22 | **Promoted to T8** (in-flight, 2026-07-08). |
| ~~CSS-neutralize ("the fix") disable toggle — **Approach A: re-emit CSS**~~ | V14, V24, V25 | **Obsolete 2026-07-21 — T10 (Approach B) landed and passed.** Approach A was the fallback for if PHP suppression failed staging; it did not. The V14 regression is dissolved rather than papered, so there is nothing left for a re-emit toggle to re-emit. Kept for provenance only. Original: Add setting or filter to disable the CSS-neutralize independently. Per V24/V25 the regression surface is Featured Image + Secondary Nav (full) and Primary Nav's `#mobile-header` wrapper (partial), so a blanket "fix OFF when GB Pro absent" default is coarser than needed — a finer option is to keep neutralize ON but re-emit only the load-bearing rules from the original `generate_disable_elements()` body: `.generate-page-header, .page-header-image, .page-header-image-single` (featured image), `#secondary-navigation` (secondary nav), and `#mobile-header` (only when `$disable_nav` and Menu Plus mobile header active), while nulling the redundant header/footer/site-navigation rules. Default: fix OFF when GB Pro absent (safe, simple); fix ON when GB Pro present. Fix-only mode becomes a user-acknowledged opt-in rather than a hard regression. V14 needs updating when implemented. **Fallback only — obsolete if T10 (Approach B) passes staging.** |
| ~~Convert CSS-only toggles to PHP suppression — **Approach B (preferred)**~~ | V14, V24, V25 | **Promoted to T10** (in-flight, 2026-07-08; gated on staging validation). Mechanism detail preserved in T10 + `docs/proto/approach-b-snippet.php`. Original analysis: PHP suppression keyed on `_generate-disable-*` meta dissolves the V14 regression rather than papering it — Secondary Nav via `has_nav_menu` filter (GP's own pattern, `class-layout.php:534`), Featured Image via `remove_action` (`featured-images.php:96,114`), `#mobile-header` via `remove_action` (`generate-menu-plus.php:1070`). Only the 3 CSS-only surfaces — don't double-suppress header/footer/top-bar. |
| ~~Featured Image Slot Active (theme) rule~~ | V7, V11, V21, V33, V34, ADR-0006 | **LANDED 2026-08-06 (GitHub issue #4, under #2).** Shipped as scoped below, with one addition the scoping did not name: the element condition derived its whole rule list by looping the signal registry and inverting each state, so it could not carry a row that is neither a signal nor disable-polarity. That prefactor (`rule_table()`, an `invert` column per row) landed first, with no behaviour change. Rule count 10 to 11; contract, mutation record and the polarity argument are V34. Not observable in a rendered response when it landed — proven at the state map, the condition boundary and the drift probe only. **Closed by T20 (issue #5, blueprint v8):** `render-surface.sh` §9 asserts the rule through a real GB Pro condition on a real block, across every reachable combination, including the kill-switch case this entry named first and no fixture had. Original scoping: **Accepted gap opened by ADR-0006, scoped as GitHub issue #4.** `featured_image_active` reports the author's post setting, so nothing reports whether **GeneratePress itself** is drawing an image. Three configurations make that false while the post-setting rule reports true: a Layout Element kill switch with nothing in its place, the Customizer's global image toggle, and a Page Header carrying its own content. Unlike the title's Meaning B this one is **cheap and safe to detect** — hook state answers it directly, provided it covers *both* render paths (the Blog module's callback on all three Customizer positions, and the theme's page-header pair for a site without that module; reading one path only is the failure B8 demonstrated on the suppression side). It is a second question about a different subject rather than a competing meaning, so it wants its own rule, not a flip. Nested inside the post-setting rule by mechanism: this plugin removes GP's image callbacks when the post toggle is set, so the slot rule is false whenever the post rule is false. No body class — see the accepted-gap section in `architecture.md`. |
| Meaning-B rule for the native title slot | V21, V30, ADR-0005 | **Accepted gap, added 2026-08-04.** After ADR-0005 no rule answers "does the **native theme slot** render the title?" A block placed *outside* a hero — duplicate-avoidance, or spacing compensation for a heading that is not there — has no signal on any page type, and `gp-no-content-title` has one name with no room to split, so CSS wanting Meaning B has nowhere to go either. Accepted because there is no known consumer (measured on both deployed sites, not assumed) and the failure direction is the visible one. **Not starting from zero if picked up:** the complete `generate_show_title` writer survey and the archive-heading suppressor table in `architecture.md` *are* the detection spec — that work is done and deliberately retained. Shape: a separate rule (slug free, no migration) reading `has_filter('generate_show_title','__return_false')` on singular and `has_action('generate_archive_title','generate_archive_title') && generate_has_default_loop()` on archives — the second conjunct is not optional, a Loop Template Block Element deletes the heading without removing any hook (`class-block.php:182`), and both deployed sites run loop templates. Naming is the real cost: two dropdown entries that are not self-explanatory without docs, which is why the two-rule shape was rejected for the main change. |
| ~~Featured image → config-replay (close V21 + V33 together)~~ | V7, V20, V21, V22, V33, B8 | **LANDED 2026-08-06 as ADR-0006** (GitHub issue #3, under #2). Scoping below is retained for provenance; three things about it turned out differently and are worth carrying forward. (1) **It is not config-replay.** The entry's title, and every framing under it, assumed the fix was the ADR-0005 shape — replay the genuine-disable configs, element key included. The element key is the one thing that could *not* be read: on real sites `_generate_disable_featured_image` is how a **relocation** is performed, because the image is drawn from hooks outside the template part and a Layout Element is the only per-page way to suppress it on an archive (the Page Hero toggle is `is_singular()`-gated). So the rule reads the per-post metabox alone, and the two title-vs-image signals deliberately **diverge** rather than converging on one pattern. (2) **The 0.2.0 archive behaviour is removed, not extended** — V22 reversed, since its only source is that same contaminated key evaluated where a silently unrendered block is least observable. (3) **The Customizer decision below was made the other way, and moved.** The `single_post_image => false` case on `hargrave` is real and this change does make the rule report active there — but that layer belongs to the sibling *slot* rule (issue #4), not to a rule named for a post setting. The "cheaper interim" (OR the three position hooks) is obsolete: no hook is read at all now. **The third open layer below was RESOLVED by B8 rather than by a survey (2026-08-05).** Scoping this entry turned up a live bug in the *suppression* half, not the detection half: the plugin removed only the theme page-header path, which the GP Premium Blog module removes itself and replaces with its own callback — so on a Blog-module-active site the per-post featured-image toggle had stopped hiding anything at all since 0.2.1. Fixed by removing all five callbacks (bug ledger B8; blueprint v6 pins the module so the harness can no longer test whichever path a site happens to have). The consequence for *this* entry: "does the metabox toggle disable the featured image" now has an answer that does not depend on module state — this plugin makes it true on both paths — so `_generate-disable-post-image` is a genuine-disable source the flip can read, and the "survey which path is live before choosing" step below is discharged. **What the flip still owes a decision, and it is not the one this entry opens with:** on `hargrave` the Customizer stores `single_post_image => false`, so hook-state currently answers **correctly** on single posts there (no image renders, rule says disabled) and a flip with no Customizer read would make it wrongly active. The flip fixes pages on that site and breaks posts. So the choice is flip-only (accept that direction, disclose it) versus flip + the Blog module's on/off conjunct (`$settings[$location.'_post_image']`, one option read behind the seam, gated on the module being active — when it is off the theme path renders and the setting is moot). The dropped conjunct in the second shape is `! $page_header_content`, which needs `generate_page_header_get_options()`; position is not needed at all, since it selects a hook and not whether an image exists. **Not theoretical — measured live 2026-08-05, which changes this entry's priority, not its shape.** On the `hargrave` clone the featured-image hook sits on `generate_after_header` while the Detector probes `generate_after_entry_header`; a page with a real thumbnail renders the image and `states()['featured_image']` returns `true`. **Featured Image Active is wrong on every singular page of that site and has been since v1.** `testbed` and `portals` store the position keys unset, so GP's shipped defaults (`inside-content` / `above-content`) produce the same misread for the stock reason. So this is not a gap awaiting a consumer: all three known installs read the signal wrong on singular right now, and the only reason it has not surfaced as a complaint is that nothing on those sites gates on the rule. Weigh the Customizer-coverage cost below against that, not against a hypothetical. **Added 2026-08-04.** The singular branch is now the only hook-state signal with a *known-wrong* direction, and it has three of them at once — V21's relocation ambiguity plus V33's two false-"disabled" cases (Customizer image position ≠ `below-title`; GP Premium Blog module deactivated). ADR-0005's fix shape applies unchanged: stop reading the poisoned hook, replay the genuine-disable configs. **One thing that made content title hard does not apply here.** The two element types share the meta key `_generate_disable_featured_image` (Layout `class-layout.php:213`, Page Hero Block `class-block.php:293`), which looks like the mirror of the title signal's two-key trap — but `layout_element_ids()` already filters `_generate_element_type = 'layout'` (`class-environment.php:113-117`), so Page Hero Block Elements are excluded **by the query**, with no key disambiguation needed and no seam change. The off-singular branch (T8/V22) is already exactly this call; the work is deleting the singular special case. Legacy Page Hero (`class-hero.php:638`) is a third relocator the flip fixes for free. **What the flip costs, and the reason this is deferred rather than done:** hook-state is currently the *only* thing detecting the Customizer layer — the `add_action` is gated on `$settings[$location.'_post_image']` **and** `! $page_header_content` (`images.php:169`, both unread as such), so a site that turns single featured images off globally, or that runs a Page Header with content, reads as disabled today and would read as active after. Weigh that against what the same read gets wrong: per V33 the position branch makes the current answer wrong on a **default** install (`defaults.php:28,35` ship `inside-content`/`above-content`, not `below-title`), so hook-state is not a working detector being traded for a lossy one — it is already wrong out of the box, and the Customizer coverage it provides is incidental to a probe that mostly misfires. **The same Customizer-layer hole exists for secondary nav** since T17 (`''` = "No Navigation", signal-map row), and for primary nav since v1 (V24) — three signals, one missing layer, and no replay path for any of them because it is a `theme_mod`, not element meta. If it is ever worth closing, close it once for all three rather than per-signal. That layer has no config-replay equivalent (it is a theme_mod, not element meta) and is an accepted gap for every other signal (V24), so accepting it here is consistent — but it is a real behaviour change on a real setting, unlike the title flip, where every affected writer was a relocation. **Third layer to resolve in the same pass, not after** — *discharged by B8, see the head of this entry; retained for the reasoning:* the post metabox `_generate-disable-post-image` is unread today and targets a *different* render path (`generate_featured_page_header*`) than the one probed, and the Blog module removes that path unconditionally when active (`images.php:164-165`) — so "does the metabox toggle disable the featured image" has no answer that is true independent of module state. Survey which path is live on the deployed sites before choosing, the way the title writer survey was done. **Cheaper interim if the flip stays deferred:** OR the three position hooks in the existing hook read, which closes V33's first direction alone and changes nothing else. Body-class note: `gp-no-featured-image` inherits whichever meaning is chosen and has no room to split (same constraint as `gp-no-content-title`, ADR-0005). |
| ~~Render-level fixture for the secondary-nav Layout Element~~ | V32, T17 | **Promoted to T18 and landed 2026-08-05.** Original scoping below. **Added 2026-08-04.** T17 is covered against the fake only. The claim the fake structurally cannot check is the one that matters: that `_generate_disable_secondary_navigation` is the key GP actually writes, and that a Layout Element carrying it really does suppress `#secondary-navigation` on both a singular page and an archive. B6 is the precedent — a fixture written in a form the consumer never matches passes silently. Shape: one `ls-el-layout-secondary-nav` element reusing the existing `department:sales` archive (blueprint v4 already resolves term IDs correctly), asserted in `render-surface.sh` alongside §6. Cheap; waiting only on the next blueprint touch. |
| Container type condition | V8 | `gp_theme_container` condition (reserved slug, V27) for full-width vs contained. GP emits native body classes already; plugin layer justified only if a GB Pro condition on container type, non-singular coverage, or Customizer-default detection is needed. None currently scoped. |
