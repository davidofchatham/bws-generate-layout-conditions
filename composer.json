{
    "name": "bws/generate-layout-conditions",
    "description": "Dev-only dependencies for bws-generate-layout-conditions (tests). The plugin itself has no Composer runtime deps.",
    "license": "GPL-2.0-or-later",
    "require-dev": {
        "phpunit/phpunit": "^11"
    },
    "scripts": {
        "test": "phpunit"
    }
}
